# middlewareUsingRMI
Go to the src directory
First you need to compile the all the files
	javac *.java

Then start the rmi registry
	start rmiregistry

Then run the server 
	java Server

Then run the Client
	java Client
